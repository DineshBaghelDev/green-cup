Frontend prototype (Vite + React).

- Run: `npm install` then `npm run dev` inside `frontend`.
- The app fetches sample data from backend at `http://localhost:4000/api/sample`.
- Paste CSV in the upload box to simulate ingestion and update leaderboard locally.
