Green Cup Backend Prototype

- Run: `node index.js` (requires Node.js)
- Endpoint: `GET /api/sample` returns a sample leaderboard computed from `sample_data/hostel_bills.csv`.
- Purpose: simple prototype to provide mock leaderboard and carbon estimates.
